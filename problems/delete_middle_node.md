---
id: delete_middle_node
category: Linked List
difficulty: Medium
englishName: Delete the Middle Node
chineseName: 删除链表的中间结点
methodName: deleteMiddleNode
javascriptFunctionName: deleteMiddleNode
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Delete the Middle Node` in the `Linked List` track. Use the title and tag `Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Delete the Middle Node", "category": "Linked List", "sample": 1}
Output: {"topic": "Delete the Middle Node", "category": "Linked List", "sample": 1}

### Example 2
Input: inputData = {"topic": "Delete the Middle Node", "category": "Linked List", "sample": 2}
Output: {"topic": "Delete the Middle Node", "category": "Linked List", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def deleteMiddleNode(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function deleteMiddleNode(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Delete the Middle Node",
        "category": "Linked List",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Delete the Middle Node",
      "category": "Linked List",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Delete the Middle Node",
        "category": "Linked List",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Delete the Middle Node",
      "category": "Linked List",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Delete the Middle Node",
        "Pointers"
      ]
    },
    "expected": [
      "Delete the Middle Node",
      "Pointers"
    ]
  }
]
```
