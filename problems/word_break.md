---
id: word_break
category: Dynamic Programming
difficulty: Medium
englishName: Word Break
chineseName: 单词拆分
methodName: wordBreak
javascriptFunctionName: wordBreak
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Word Break` in the `Dynamic Programming` track. Use the title and tag `1-D DP` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Word Break", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Word Break", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Word Break", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Word Break", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def wordBreak(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function wordBreak(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Word Break",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Word Break",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Word Break",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Word Break",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Word Break",
        "1-D DP"
      ]
    },
    "expected": [
      "Word Break",
      "1-D DP"
    ]
  }
]
```
