---
id: move_zeroes
category: Two Pointers
difficulty: Easy
englishName: Move Zeroes
chineseName: 移动零
methodName: moveZeroes
javascriptFunctionName: moveZeroes
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Move Zeroes` in the `Two Pointers` track. Use the title and tag `In-place` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Move Zeroes", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Move Zeroes", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Move Zeroes", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Move Zeroes", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def moveZeroes(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function moveZeroes(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Move Zeroes",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Move Zeroes",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Move Zeroes",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Move Zeroes",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Move Zeroes",
        "In-place"
      ]
    },
    "expected": [
      "Move Zeroes",
      "In-place"
    ]
  }
]
```
