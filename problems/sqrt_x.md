---
id: sqrt_x
category: Binary Search
difficulty: Easy
englishName: Sqrt(x)
chineseName: x 的平方根
methodName: sqrtX
javascriptFunctionName: sqrtX
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Sqrt(x)` in the `Binary Search` track. Use the title and tag `Binary Search` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Sqrt(x)", "category": "Binary Search", "sample": 1}
Output: {"topic": "Sqrt(x)", "category": "Binary Search", "sample": 1}

### Example 2
Input: inputData = {"topic": "Sqrt(x)", "category": "Binary Search", "sample": 2}
Output: {"topic": "Sqrt(x)", "category": "Binary Search", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def sqrtX(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function sqrtX(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Sqrt(x)",
        "category": "Binary Search",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Sqrt(x)",
      "category": "Binary Search",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Sqrt(x)",
        "category": "Binary Search",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Sqrt(x)",
      "category": "Binary Search",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Sqrt(x)",
        "Binary Search"
      ]
    },
    "expected": [
      "Sqrt(x)",
      "Binary Search"
    ]
  }
]
```
