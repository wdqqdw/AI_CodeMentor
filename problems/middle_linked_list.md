---
id: middle_linked_list
category: Two Pointers
difficulty: Easy
englishName: Middle of the Linked List
chineseName: 链表的中间结点
methodName: middleLinkedList
javascriptFunctionName: middleLinkedList
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Middle of the Linked List` in the `Two Pointers` track. Use the title and tag `Fast & Slow` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Middle of the Linked List", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Middle of the Linked List", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Middle of the Linked List", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Middle of the Linked List", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def middleLinkedList(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function middleLinkedList(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Middle of the Linked List",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Middle of the Linked List",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Middle of the Linked List",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Middle of the Linked List",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Middle of the Linked List",
        "Fast & Slow"
      ]
    },
    "expected": [
      "Middle of the Linked List",
      "Fast & Slow"
    ]
  }
]
```
