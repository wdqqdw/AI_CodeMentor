---
id: maximum_average_subarray
category: Sliding Window
difficulty: Easy
englishName: Maximum Average Subarray I
chineseName: 子数组最大平均数 I
methodName: maximumAverageSubarray
javascriptFunctionName: maximumAverageSubarray
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Maximum Average Subarray I` in the `Sliding Window` track. Use the title and tag `Window` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Maximum Average Subarray I", "category": "Sliding Window", "sample": 1}
Output: {"topic": "Maximum Average Subarray I", "category": "Sliding Window", "sample": 1}

### Example 2
Input: inputData = {"topic": "Maximum Average Subarray I", "category": "Sliding Window", "sample": 2}
Output: {"topic": "Maximum Average Subarray I", "category": "Sliding Window", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def maximumAverageSubarray(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function maximumAverageSubarray(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Maximum Average Subarray I",
        "category": "Sliding Window",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Maximum Average Subarray I",
      "category": "Sliding Window",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Maximum Average Subarray I",
        "category": "Sliding Window",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Maximum Average Subarray I",
      "category": "Sliding Window",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Maximum Average Subarray I",
        "Window"
      ]
    },
    "expected": [
      "Maximum Average Subarray I",
      "Window"
    ]
  }
]
```
