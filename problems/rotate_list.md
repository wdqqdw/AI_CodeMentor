---
id: rotate_list
category: Linked List
difficulty: Medium
englishName: Rotate List
chineseName: 旋转链表
methodName: rotateList
javascriptFunctionName: rotateList
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Rotate List` in the `Linked List` track. Use the title and tag `Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Rotate List", "category": "Linked List", "sample": 1}
Output: {"topic": "Rotate List", "category": "Linked List", "sample": 1}

### Example 2
Input: inputData = {"topic": "Rotate List", "category": "Linked List", "sample": 2}
Output: {"topic": "Rotate List", "category": "Linked List", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def rotateList(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function rotateList(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Rotate List",
        "category": "Linked List",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Rotate List",
      "category": "Linked List",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Rotate List",
        "category": "Linked List",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Rotate List",
      "category": "Linked List",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Rotate List",
        "Pointers"
      ]
    },
    "expected": [
      "Rotate List",
      "Pointers"
    ]
  }
]
```
