---
id: trapping_rain_water
category: Two Pointers
difficulty: Hard
englishName: Trapping Rain Water
chineseName: 接雨水
methodName: trappingRainWater
javascriptFunctionName: trappingRainWater
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Trapping Rain Water` in the `Two Pointers` track. Use the title and tag `Two Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Trapping Rain Water", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Trapping Rain Water", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Trapping Rain Water", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Trapping Rain Water", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def trappingRainWater(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function trappingRainWater(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Trapping Rain Water",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Trapping Rain Water",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Trapping Rain Water",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Trapping Rain Water",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Trapping Rain Water",
        "Two Pointers"
      ]
    },
    "expected": [
      "Trapping Rain Water",
      "Two Pointers"
    ]
  }
]
```
