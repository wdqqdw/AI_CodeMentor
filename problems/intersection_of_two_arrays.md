---
id: intersection_of_two_arrays
category: Array & Hashing
difficulty: Easy
englishName: Intersection of Two Arrays
chineseName: 两个数组的交集
methodName: intersectionOfTwoArrays
javascriptFunctionName: intersectionOfTwoArrays
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Intersection of Two Arrays` in the `Array & Hashing` track. Use the title and tag `Set` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Intersection of Two Arrays", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Intersection of Two Arrays", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Intersection of Two Arrays", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Intersection of Two Arrays", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def intersectionOfTwoArrays(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function intersectionOfTwoArrays(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Intersection of Two Arrays",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Intersection of Two Arrays",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Intersection of Two Arrays",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Intersection of Two Arrays",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Intersection of Two Arrays",
        "Set"
      ]
    },
    "expected": [
      "Intersection of Two Arrays",
      "Set"
    ]
  }
]
```
