---
id: minimum_size_subarray_sum
category: Two Pointers
difficulty: Medium
englishName: Minimum Size Subarray Sum
chineseName: 长度最小的子数组
methodName: minimumSizeSubarraySum
javascriptFunctionName: minimumSizeSubarraySum
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Minimum Size Subarray Sum` in the `Two Pointers` track. Use the title and tag `Window` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Minimum Size Subarray Sum", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Minimum Size Subarray Sum", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Minimum Size Subarray Sum", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Minimum Size Subarray Sum", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def minimumSizeSubarraySum(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function minimumSizeSubarraySum(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Minimum Size Subarray Sum",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Minimum Size Subarray Sum",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Minimum Size Subarray Sum",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Minimum Size Subarray Sum",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Minimum Size Subarray Sum",
        "Window"
      ]
    },
    "expected": [
      "Minimum Size Subarray Sum",
      "Window"
    ]
  }
]
```
