---
id: time_based_key_value_store
category: Binary Search
difficulty: Medium
englishName: Time Based Key-Value Store
chineseName: 基于时间的键值存储
methodName: timeBasedKeyValueStore
javascriptFunctionName: timeBasedKeyValueStore
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Time Based Key-Value Store` in the `Binary Search` track. Use the title and tag `Design` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Time Based Key-Value Store", "category": "Binary Search", "sample": 1}
Output: {"topic": "Time Based Key-Value Store", "category": "Binary Search", "sample": 1}

### Example 2
Input: inputData = {"topic": "Time Based Key-Value Store", "category": "Binary Search", "sample": 2}
Output: {"topic": "Time Based Key-Value Store", "category": "Binary Search", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def timeBasedKeyValueStore(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function timeBasedKeyValueStore(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Time Based Key-Value Store",
        "category": "Binary Search",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Time Based Key-Value Store",
      "category": "Binary Search",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Time Based Key-Value Store",
        "category": "Binary Search",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Time Based Key-Value Store",
      "category": "Binary Search",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Time Based Key-Value Store",
        "Design"
      ]
    },
    "expected": [
      "Time Based Key-Value Store",
      "Design"
    ]
  }
]
```
