---
id: interleaving_string
category: Dynamic Programming
difficulty: Medium
englishName: Interleaving String
chineseName: 交错字符串
methodName: interleavingString
javascriptFunctionName: interleavingString
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Interleaving String` in the `Dynamic Programming` track. Use the title and tag `2-D DP` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Interleaving String", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Interleaving String", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Interleaving String", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Interleaving String", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def interleavingString(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function interleavingString(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Interleaving String",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Interleaving String",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Interleaving String",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Interleaving String",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Interleaving String",
        "2-D DP"
      ]
    },
    "expected": [
      "Interleaving String",
      "2-D DP"
    ]
  }
]
```
