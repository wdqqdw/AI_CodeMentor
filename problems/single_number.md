---
id: single_number
category: Array & Hashing
difficulty: Easy
englishName: Single Number
chineseName: 只出现一次的数字
methodName: singleNumber
javascriptFunctionName: singleNumber
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Single Number` in the `Array & Hashing` track. Use the title and tag `Bit` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Single Number", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Single Number", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Single Number", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Single Number", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def singleNumber(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function singleNumber(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Single Number",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Single Number",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Single Number",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Single Number",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Single Number",
        "Bit"
      ]
    },
    "expected": [
      "Single Number",
      "Bit"
    ]
  }
]
```
