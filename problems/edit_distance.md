---
id: edit_distance
category: Dynamic Programming
difficulty: Hard
englishName: Edit Distance
chineseName: 编辑距离
methodName: editDistance
javascriptFunctionName: editDistance
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Edit Distance` in the `Dynamic Programming` track. Use the title and tag `2-D DP` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Edit Distance", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Edit Distance", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Edit Distance", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Edit Distance", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def editDistance(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function editDistance(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Edit Distance",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Edit Distance",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Edit Distance",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Edit Distance",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Edit Distance",
        "2-D DP"
      ]
    },
    "expected": [
      "Edit Distance",
      "2-D DP"
    ]
  }
]
```
