---
id: swim_in_rising_water
category: Heap / Priority Queue
difficulty: Hard
englishName: Swim in Rising Water
chineseName: 水位上升的泳池中游泳
methodName: swimInRisingWater
javascriptFunctionName: swimInRisingWater
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Swim in Rising Water` in the `Heap / Priority Queue` track. Use the title and tag `Heap` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Swim in Rising Water", "category": "Heap / Priority Queue", "sample": 1}
Output: {"topic": "Swim in Rising Water", "category": "Heap / Priority Queue", "sample": 1}

### Example 2
Input: inputData = {"topic": "Swim in Rising Water", "category": "Heap / Priority Queue", "sample": 2}
Output: {"topic": "Swim in Rising Water", "category": "Heap / Priority Queue", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def swimInRisingWater(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function swimInRisingWater(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Swim in Rising Water",
        "category": "Heap / Priority Queue",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Swim in Rising Water",
      "category": "Heap / Priority Queue",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Swim in Rising Water",
        "category": "Heap / Priority Queue",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Swim in Rising Water",
      "category": "Heap / Priority Queue",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Swim in Rising Water",
        "Heap"
      ]
    },
    "expected": [
      "Swim in Rising Water",
      "Heap"
    ]
  }
]
```
