---
id: reverse_nodes_k_group
category: Linked List
difficulty: Hard
englishName: Reverse Nodes in k-Group
chineseName: K 个一组翻转链表
methodName: reverseNodesKGroup
javascriptFunctionName: reverseNodesKGroup
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Reverse Nodes in k-Group` in the `Linked List` track. Use the title and tag `Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Reverse Nodes in k-Group", "category": "Linked List", "sample": 1}
Output: {"topic": "Reverse Nodes in k-Group", "category": "Linked List", "sample": 1}

### Example 2
Input: inputData = {"topic": "Reverse Nodes in k-Group", "category": "Linked List", "sample": 2}
Output: {"topic": "Reverse Nodes in k-Group", "category": "Linked List", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def reverseNodesKGroup(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function reverseNodesKGroup(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Reverse Nodes in k-Group",
        "category": "Linked List",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Reverse Nodes in k-Group",
      "category": "Linked List",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Reverse Nodes in k-Group",
        "category": "Linked List",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Reverse Nodes in k-Group",
      "category": "Linked List",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Reverse Nodes in k-Group",
        "Pointers"
      ]
    },
    "expected": [
      "Reverse Nodes in k-Group",
      "Pointers"
    ]
  }
]
```
