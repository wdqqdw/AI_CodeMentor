---
id: remove_k_digits
category: Stack
difficulty: Medium
englishName: Remove K Digits
chineseName: 移掉 K 位数字
methodName: removeKDigits
javascriptFunctionName: removeKDigits
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Remove K Digits` in the `Stack` track. Use the title and tag `Monotonic Stack` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Remove K Digits", "category": "Stack", "sample": 1}
Output: {"topic": "Remove K Digits", "category": "Stack", "sample": 1}

### Example 2
Input: inputData = {"topic": "Remove K Digits", "category": "Stack", "sample": 2}
Output: {"topic": "Remove K Digits", "category": "Stack", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def removeKDigits(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function removeKDigits(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Remove K Digits",
        "category": "Stack",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Remove K Digits",
      "category": "Stack",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Remove K Digits",
        "category": "Stack",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Remove K Digits",
      "category": "Stack",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Remove K Digits",
        "Monotonic Stack"
      ]
    },
    "expected": [
      "Remove K Digits",
      "Monotonic Stack"
    ]
  }
]
```
