---
id: permutation_in_string
category: Sliding Window
difficulty: Medium
englishName: Permutation in String
chineseName: 字符串的排列
methodName: permutationInString
javascriptFunctionName: permutationInString
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Permutation in String` in the `Sliding Window` track. Use the title and tag `Window` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Permutation in String", "category": "Sliding Window", "sample": 1}
Output: {"topic": "Permutation in String", "category": "Sliding Window", "sample": 1}

### Example 2
Input: inputData = {"topic": "Permutation in String", "category": "Sliding Window", "sample": 2}
Output: {"topic": "Permutation in String", "category": "Sliding Window", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def permutationInString(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function permutationInString(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Permutation in String",
        "category": "Sliding Window",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Permutation in String",
      "category": "Sliding Window",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Permutation in String",
        "category": "Sliding Window",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Permutation in String",
      "category": "Sliding Window",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Permutation in String",
        "Window"
      ]
    },
    "expected": [
      "Permutation in String",
      "Window"
    ]
  }
]
```
