---
id: missing_number
category: Array & Hashing
difficulty: Easy
englishName: Missing Number
chineseName: 丢失的数字
methodName: missingNumber
javascriptFunctionName: missingNumber
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Missing Number` in the `Array & Hashing` track. Use the title and tag `Math` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Missing Number", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Missing Number", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Missing Number", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Missing Number", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def missingNumber(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function missingNumber(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Missing Number",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Missing Number",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Missing Number",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Missing Number",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Missing Number",
        "Math"
      ]
    },
    "expected": [
      "Missing Number",
      "Math"
    ]
  }
]
```
