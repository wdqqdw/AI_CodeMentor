---
id: ship_packages_days
category: Binary Search
difficulty: Medium
englishName: Capacity To Ship Packages Within D Days
chineseName: 在 D 天内送达包裹的能力
methodName: shipPackagesDays
javascriptFunctionName: shipPackagesDays
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Capacity To Ship Packages Within D Days` in the `Binary Search` track. Use the title and tag `Answer Search` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Capacity To Ship Packages Within D Days", "category": "Binary Search", "sample": 1}
Output: {"topic": "Capacity To Ship Packages Within D Days", "category": "Binary Search", "sample": 1}

### Example 2
Input: inputData = {"topic": "Capacity To Ship Packages Within D Days", "category": "Binary Search", "sample": 2}
Output: {"topic": "Capacity To Ship Packages Within D Days", "category": "Binary Search", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def shipPackagesDays(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function shipPackagesDays(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Capacity To Ship Packages Within D Days",
        "category": "Binary Search",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Capacity To Ship Packages Within D Days",
      "category": "Binary Search",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Capacity To Ship Packages Within D Days",
        "category": "Binary Search",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Capacity To Ship Packages Within D Days",
      "category": "Binary Search",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Capacity To Ship Packages Within D Days",
        "Answer Search"
      ]
    },
    "expected": [
      "Capacity To Ship Packages Within D Days",
      "Answer Search"
    ]
  }
]
```
