---
id: copy_random_pointer
category: Linked List
difficulty: Medium
englishName: Copy List with Random Pointer
chineseName: 复制带随机指针的链表
methodName: copyRandomPointer
javascriptFunctionName: copyRandomPointer
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Copy List with Random Pointer` in the `Linked List` track. Use the title and tag `Hash Map` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Copy List with Random Pointer", "category": "Linked List", "sample": 1}
Output: {"topic": "Copy List with Random Pointer", "category": "Linked List", "sample": 1}

### Example 2
Input: inputData = {"topic": "Copy List with Random Pointer", "category": "Linked List", "sample": 2}
Output: {"topic": "Copy List with Random Pointer", "category": "Linked List", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def copyRandomPointer(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function copyRandomPointer(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Copy List with Random Pointer",
        "category": "Linked List",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Copy List with Random Pointer",
      "category": "Linked List",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Copy List with Random Pointer",
        "category": "Linked List",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Copy List with Random Pointer",
      "category": "Linked List",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Copy List with Random Pointer",
        "Hash Map"
      ]
    },
    "expected": [
      "Copy List with Random Pointer",
      "Hash Map"
    ]
  }
]
```
