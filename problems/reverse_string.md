---
id: reverse_string
category: Two Pointers
difficulty: Easy
englishName: Reverse String
chineseName: 反转字符串
methodName: reverseString
javascriptFunctionName: reverseString
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Reverse String` in the `Two Pointers` track. Use the title and tag `Two Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Reverse String", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Reverse String", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Reverse String", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Reverse String", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def reverseString(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function reverseString(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Reverse String",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Reverse String",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Reverse String",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Reverse String",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Reverse String",
        "Two Pointers"
      ]
    },
    "expected": [
      "Reverse String",
      "Two Pointers"
    ]
  }
]
```
