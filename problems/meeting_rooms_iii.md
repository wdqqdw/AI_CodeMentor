---
id: meeting_rooms_iii
category: Heap / Priority Queue
difficulty: Hard
englishName: Meeting Rooms III
chineseName: 会议室 III
methodName: meetingRoomsIii
javascriptFunctionName: meetingRoomsIii
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Meeting Rooms III` in the `Heap / Priority Queue` track. Use the title and tag `Heap` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Meeting Rooms III", "category": "Heap / Priority Queue", "sample": 1}
Output: {"topic": "Meeting Rooms III", "category": "Heap / Priority Queue", "sample": 1}

### Example 2
Input: inputData = {"topic": "Meeting Rooms III", "category": "Heap / Priority Queue", "sample": 2}
Output: {"topic": "Meeting Rooms III", "category": "Heap / Priority Queue", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def meetingRoomsIii(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function meetingRoomsIii(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Meeting Rooms III",
        "category": "Heap / Priority Queue",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Meeting Rooms III",
      "category": "Heap / Priority Queue",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Meeting Rooms III",
        "category": "Heap / Priority Queue",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Meeting Rooms III",
      "category": "Heap / Priority Queue",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Meeting Rooms III",
        "Heap"
      ]
    },
    "expected": [
      "Meeting Rooms III",
      "Heap"
    ]
  }
]
```
