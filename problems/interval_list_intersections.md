---
id: interval_list_intersections
category: Intervals
difficulty: Medium
englishName: Interval List Intersections
chineseName: 区间列表的交集
methodName: intervalListIntersections
javascriptFunctionName: intervalListIntersections
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Interval List Intersections` in the `Intervals` track. Use the title and tag `Two Pointers` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Interval List Intersections", "category": "Intervals", "sample": 1}
Output: {"topic": "Interval List Intersections", "category": "Intervals", "sample": 1}

### Example 2
Input: inputData = {"topic": "Interval List Intersections", "category": "Intervals", "sample": 2}
Output: {"topic": "Interval List Intersections", "category": "Intervals", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def intervalListIntersections(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function intervalListIntersections(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Interval List Intersections",
        "category": "Intervals",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Interval List Intersections",
      "category": "Intervals",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Interval List Intersections",
        "category": "Intervals",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Interval List Intersections",
      "category": "Intervals",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Interval List Intersections",
        "Two Pointers"
      ]
    },
    "expected": [
      "Interval List Intersections",
      "Two Pointers"
    ]
  }
]
```
