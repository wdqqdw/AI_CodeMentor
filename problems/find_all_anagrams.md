---
id: find_all_anagrams
category: Array & Hashing
difficulty: Medium
englishName: Find All Anagrams in a String
chineseName: 找到字符串中所有字母异位词
methodName: findAllAnagrams
javascriptFunctionName: findAllAnagrams
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Find All Anagrams in a String` in the `Array & Hashing` track. Use the title and tag `Hash Map` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Find All Anagrams in a String", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Find All Anagrams in a String", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Find All Anagrams in a String", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Find All Anagrams in a String", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def findAllAnagrams(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function findAllAnagrams(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Find All Anagrams in a String",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Find All Anagrams in a String",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Find All Anagrams in a String",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Find All Anagrams in a String",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Find All Anagrams in a String",
        "Hash Map"
      ]
    },
    "expected": [
      "Find All Anagrams in a String",
      "Hash Map"
    ]
  }
]
```
