---
id: letter_combinations_phone
category: Backtracking
difficulty: Medium
englishName: Letter Combinations of a Phone Number
chineseName: 电话号码的字母组合
methodName: letterCombinationsPhone
javascriptFunctionName: letterCombinationsPhone
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Letter Combinations of a Phone Number` in the `Backtracking` track. Use the title and tag `DFS` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Letter Combinations of a Phone Number", "category": "Backtracking", "sample": 1}
Output: {"topic": "Letter Combinations of a Phone Number", "category": "Backtracking", "sample": 1}

### Example 2
Input: inputData = {"topic": "Letter Combinations of a Phone Number", "category": "Backtracking", "sample": 2}
Output: {"topic": "Letter Combinations of a Phone Number", "category": "Backtracking", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def letterCombinationsPhone(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function letterCombinationsPhone(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Letter Combinations of a Phone Number",
        "category": "Backtracking",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Letter Combinations of a Phone Number",
      "category": "Backtracking",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Letter Combinations of a Phone Number",
        "category": "Backtracking",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Letter Combinations of a Phone Number",
      "category": "Backtracking",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Letter Combinations of a Phone Number",
        "DFS"
      ]
    },
    "expected": [
      "Letter Combinations of a Phone Number",
      "DFS"
    ]
  }
]
```
