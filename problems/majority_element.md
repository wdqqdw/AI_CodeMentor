---
id: majority_element
category: Array & Hashing
difficulty: Easy
englishName: Majority Element
chineseName: 多数元素
methodName: majorityElement
javascriptFunctionName: majorityElement
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Majority Element` in the `Array & Hashing` track. Use the title and tag `Counting` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Majority Element", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Majority Element", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Majority Element", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Majority Element", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def majorityElement(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function majorityElement(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Majority Element",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Majority Element",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Majority Element",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Majority Element",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Majority Element",
        "Counting"
      ]
    },
    "expected": [
      "Majority Element",
      "Counting"
    ]
  }
]
```
