---
id: partition_labels
category: Two Pointers
difficulty: Medium
englishName: Partition Labels
chineseName: 划分字母区间
methodName: partitionLabels
javascriptFunctionName: partitionLabels
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Partition Labels` in the `Two Pointers` track. Use the title and tag `Greedy` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Partition Labels", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Partition Labels", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Partition Labels", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Partition Labels", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def partitionLabels(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function partitionLabels(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Partition Labels",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Partition Labels",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Partition Labels",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Partition Labels",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Partition Labels",
        "Greedy"
      ]
    },
    "expected": [
      "Partition Labels",
      "Greedy"
    ]
  }
]
```
