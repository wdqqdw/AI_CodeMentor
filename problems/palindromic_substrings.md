---
id: palindromic_substrings
category: Dynamic Programming
difficulty: Medium
englishName: Palindromic Substrings
chineseName: 回文子串
methodName: palindromicSubstrings
javascriptFunctionName: palindromicSubstrings
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Palindromic Substrings` in the `Dynamic Programming` track. Use the title and tag `String DP` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Palindromic Substrings", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Palindromic Substrings", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Palindromic Substrings", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Palindromic Substrings", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def palindromicSubstrings(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function palindromicSubstrings(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Palindromic Substrings",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Palindromic Substrings",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Palindromic Substrings",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Palindromic Substrings",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Palindromic Substrings",
        "String DP"
      ]
    },
    "expected": [
      "Palindromic Substrings",
      "String DP"
    ]
  }
]
```
