---
id: remove_nth_node_from_end
category: Two Pointers
difficulty: Medium
englishName: Remove Nth Node From End
chineseName: 删除倒数第 N 个结点
methodName: removeNthNodeFromEnd
javascriptFunctionName: removeNthNodeFromEnd
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Remove Nth Node From End` in the `Two Pointers` track. Use the title and tag `Fast & Slow` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Remove Nth Node From End", "category": "Two Pointers", "sample": 1}
Output: {"topic": "Remove Nth Node From End", "category": "Two Pointers", "sample": 1}

### Example 2
Input: inputData = {"topic": "Remove Nth Node From End", "category": "Two Pointers", "sample": 2}
Output: {"topic": "Remove Nth Node From End", "category": "Two Pointers", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def removeNthNodeFromEnd(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function removeNthNodeFromEnd(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Remove Nth Node From End",
        "category": "Two Pointers",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Remove Nth Node From End",
      "category": "Two Pointers",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Remove Nth Node From End",
        "category": "Two Pointers",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Remove Nth Node From End",
      "category": "Two Pointers",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Remove Nth Node From End",
        "Fast & Slow"
      ]
    },
    "expected": [
      "Remove Nth Node From End",
      "Fast & Slow"
    ]
  }
]
```
