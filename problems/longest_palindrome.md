---
id: longest_palindrome
category: Array & Hashing
difficulty: Medium
englishName: Longest Palindrome
chineseName: 最长回文串
methodName: longestPalindrome
javascriptFunctionName: longestPalindrome
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Longest Palindrome` in the `Array & Hashing` track. Use the title and tag `Counting` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Longest Palindrome", "category": "Array & Hashing", "sample": 1}
Output: {"topic": "Longest Palindrome", "category": "Array & Hashing", "sample": 1}

### Example 2
Input: inputData = {"topic": "Longest Palindrome", "category": "Array & Hashing", "sample": 2}
Output: {"topic": "Longest Palindrome", "category": "Array & Hashing", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def longestPalindrome(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function longestPalindrome(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Longest Palindrome",
        "category": "Array & Hashing",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Longest Palindrome",
      "category": "Array & Hashing",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Longest Palindrome",
        "category": "Array & Hashing",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Longest Palindrome",
      "category": "Array & Hashing",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Longest Palindrome",
        "Counting"
      ]
    },
    "expected": [
      "Longest Palindrome",
      "Counting"
    ]
  }
]
```
