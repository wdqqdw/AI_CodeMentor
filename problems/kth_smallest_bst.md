---
id: kth_smallest_bst
category: Trees
difficulty: Medium
englishName: Kth Smallest Element in a BST
chineseName: 二叉搜索树中第 K 小的元素
methodName: kthSmallestBst
javascriptFunctionName: kthSmallestBst
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Kth Smallest Element in a BST` in the `Trees` track. Use the title and tag `Inorder` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Kth Smallest Element in a BST", "category": "Trees", "sample": 1}
Output: {"topic": "Kth Smallest Element in a BST", "category": "Trees", "sample": 1}

### Example 2
Input: inputData = {"topic": "Kth Smallest Element in a BST", "category": "Trees", "sample": 2}
Output: {"topic": "Kth Smallest Element in a BST", "category": "Trees", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def kthSmallestBst(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function kthSmallestBst(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Kth Smallest Element in a BST",
        "category": "Trees",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Kth Smallest Element in a BST",
      "category": "Trees",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Kth Smallest Element in a BST",
        "category": "Trees",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Kth Smallest Element in a BST",
      "category": "Trees",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Kth Smallest Element in a BST",
        "Inorder"
      ]
    },
    "expected": [
      "Kth Smallest Element in a BST",
      "Inorder"
    ]
  }
]
```
