---
id: unique_paths
category: Dynamic Programming
difficulty: Medium
englishName: Unique Paths
chineseName: 不同路径
methodName: uniquePaths
javascriptFunctionName: uniquePaths
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Unique Paths` in the `Dynamic Programming` track. Use the title and tag `2-D DP` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Unique Paths", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Unique Paths", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Unique Paths", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Unique Paths", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def uniquePaths(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function uniquePaths(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Unique Paths",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Unique Paths",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Unique Paths",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Unique Paths",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Unique Paths",
        "2-D DP"
      ]
    },
    "expected": [
      "Unique Paths",
      "2-D DP"
    ]
  }
]
```
