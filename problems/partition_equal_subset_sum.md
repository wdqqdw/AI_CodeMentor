---
id: partition_equal_subset_sum
category: Dynamic Programming
difficulty: Medium
englishName: Partition Equal Subset Sum
chineseName: 分割等和子集
methodName: partitionEqualSubsetSum
javascriptFunctionName: partitionEqualSubsetSum
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Partition Equal Subset Sum` in the `Dynamic Programming` track. Use the title and tag `Knapsack` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Partition Equal Subset Sum", "category": "Dynamic Programming", "sample": 1}
Output: {"topic": "Partition Equal Subset Sum", "category": "Dynamic Programming", "sample": 1}

### Example 2
Input: inputData = {"topic": "Partition Equal Subset Sum", "category": "Dynamic Programming", "sample": 2}
Output: {"topic": "Partition Equal Subset Sum", "category": "Dynamic Programming", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def partitionEqualSubsetSum(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function partitionEqualSubsetSum(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Partition Equal Subset Sum",
        "category": "Dynamic Programming",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Partition Equal Subset Sum",
      "category": "Dynamic Programming",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Partition Equal Subset Sum",
        "category": "Dynamic Programming",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Partition Equal Subset Sum",
      "category": "Dynamic Programming",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Partition Equal Subset Sum",
        "Knapsack"
      ]
    },
    "expected": [
      "Partition Equal Subset Sum",
      "Knapsack"
    ]
  }
]
```
