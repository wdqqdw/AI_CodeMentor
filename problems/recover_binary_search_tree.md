---
id: recover_binary_search_tree
category: Trees
difficulty: Hard
englishName: Recover Binary Search Tree
chineseName: 恢复二叉搜索树
methodName: recoverBinarySearchTree
javascriptFunctionName: recoverBinarySearchTree
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Recover Binary Search Tree` in the `Trees` track. Use the title and tag `Inorder` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Recover Binary Search Tree", "category": "Trees", "sample": 1}
Output: {"topic": "Recover Binary Search Tree", "category": "Trees", "sample": 1}

### Example 2
Input: inputData = {"topic": "Recover Binary Search Tree", "category": "Trees", "sample": 2}
Output: {"topic": "Recover Binary Search Tree", "category": "Trees", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def recoverBinarySearchTree(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function recoverBinarySearchTree(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Recover Binary Search Tree",
        "category": "Trees",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Recover Binary Search Tree",
      "category": "Trees",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Recover Binary Search Tree",
        "category": "Trees",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Recover Binary Search Tree",
      "category": "Trees",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Recover Binary Search Tree",
        "Inorder"
      ]
    },
    "expected": [
      "Recover Binary Search Tree",
      "Inorder"
    ]
  }
]
```
