---
id: seat_reservation_manager
category: Heap / Priority Queue
difficulty: Medium
englishName: Seat Reservation Manager
chineseName: 座位预约管理系统
methodName: seatReservationManager
javascriptFunctionName: seatReservationManager
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Seat Reservation Manager` in the `Heap / Priority Queue` track. Use the title and tag `Heap` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Seat Reservation Manager", "category": "Heap / Priority Queue", "sample": 1}
Output: {"topic": "Seat Reservation Manager", "category": "Heap / Priority Queue", "sample": 1}

### Example 2
Input: inputData = {"topic": "Seat Reservation Manager", "category": "Heap / Priority Queue", "sample": 2}
Output: {"topic": "Seat Reservation Manager", "category": "Heap / Priority Queue", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def seatReservationManager(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function seatReservationManager(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Seat Reservation Manager",
        "category": "Heap / Priority Queue",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Seat Reservation Manager",
      "category": "Heap / Priority Queue",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Seat Reservation Manager",
        "category": "Heap / Priority Queue",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Seat Reservation Manager",
      "category": "Heap / Priority Queue",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Seat Reservation Manager",
        "Heap"
      ]
    },
    "expected": [
      "Seat Reservation Manager",
      "Heap"
    ]
  }
]
```
