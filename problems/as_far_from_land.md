---
id: as_far_from_land
category: Graphs
difficulty: Medium
englishName: As Far from Land as Possible
chineseName: 地图分析
methodName: asFarFromLand
javascriptFunctionName: asFarFromLand
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `As Far from Land as Possible` in the `Graphs` track. Use the title and tag `BFS` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "As Far from Land as Possible", "category": "Graphs", "sample": 1}
Output: {"topic": "As Far from Land as Possible", "category": "Graphs", "sample": 1}

### Example 2
Input: inputData = {"topic": "As Far from Land as Possible", "category": "Graphs", "sample": 2}
Output: {"topic": "As Far from Land as Possible", "category": "Graphs", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def asFarFromLand(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function asFarFromLand(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "As Far from Land as Possible",
        "category": "Graphs",
        "sample": 1
      }
    },
    "expected": {
      "topic": "As Far from Land as Possible",
      "category": "Graphs",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "As Far from Land as Possible",
        "category": "Graphs",
        "sample": 2
      }
    },
    "expected": {
      "topic": "As Far from Land as Possible",
      "category": "Graphs",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "As Far from Land as Possible",
        "BFS"
      ]
    },
    "expected": [
      "As Far from Land as Possible",
      "BFS"
    ]
  }
]
```
