---
id: backspace_string_compare
category: Stack
difficulty: Easy
englishName: Backspace String Compare
chineseName: 比较含退格的字符串
methodName: backspaceStringCompare
javascriptFunctionName: backspaceStringCompare
validation: exact
inputParams: inputData
visibleTestCount: 2
---

## Description

This is a draft practice page for `Backspace String Compare` in the `Stack` track. Use the title and tag `Stack` as the problem clue. The detailed statement and stronger tests can be refined later while the page remains clickable and usable in the tutor workflow.

## Examples

### Example 1
Input: inputData = {"topic": "Backspace String Compare", "category": "Stack", "sample": 1}
Output: {"topic": "Backspace String Compare", "category": "Stack", "sample": 1}

### Example 2
Input: inputData = {"topic": "Backspace String Compare", "category": "Stack", "sample": 2}
Output: {"topic": "Backspace String Compare", "category": "Stack", "sample": 2}

## Starter Code - Python

```python
from typing import Any

class Solution:
    def backspaceStringCompare(self, inputData: Any) -> Any:
        # Input:
        #   inputData: draft payload for this practice page
        # Output:
        #   Return the required result for this problem.
        #
        # This draft page keeps the workflow available while
        # the detailed statement and hidden tests are refined.
        pass
```

## Starter Code - JavaScript

```javascript
function backspaceStringCompare(inputData) {
  // Input:
  //   inputData: draft payload for this practice page
  // Output:
  //   Return the required result for this problem.
}
```

## Tests

```json
[
  {
    "id": "case-1",
    "input": {
      "inputData": {
        "topic": "Backspace String Compare",
        "category": "Stack",
        "sample": 1
      }
    },
    "expected": {
      "topic": "Backspace String Compare",
      "category": "Stack",
      "sample": 1
    }
  },
  {
    "id": "case-2",
    "input": {
      "inputData": {
        "topic": "Backspace String Compare",
        "category": "Stack",
        "sample": 2
      }
    },
    "expected": {
      "topic": "Backspace String Compare",
      "category": "Stack",
      "sample": 2
    }
  },
  {
    "id": "case-3",
    "input": {
      "inputData": [
        "Backspace String Compare",
        "Stack"
      ]
    },
    "expected": [
      "Backspace String Compare",
      "Stack"
    ]
  }
]
```
